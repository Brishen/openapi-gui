# TODOs

## UI / Editor Enhancements
- [x] **Drag-and-Drop Reordering**: Allow users to drag-and-drop properties to reorder them in the `ObjectFields` list.
- [x] **Dedicated Enum Editor**: Create a rich UI specifically for managing `enum` values, making it easier to add, remove, and document enum options.
- [x] **Dark Mode**: Add a dark mode option to `styles.css` for better developer experience.

## Code & Schema Generation
- [x] **Mock Data Generator**: Generate example JSON payloads that satisfy the current schema to allow users to visualize the shape of the data they are asking for.
